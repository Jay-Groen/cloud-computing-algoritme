steps:
  # Build the Docker image
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'build'
      - '-t'
      - 'europe-west4-docker.pkg.dev/cloud-computing-resit-90139/trainingsapp/trainingsapp:latest'
      - '.'

  # Push to Artifact Registry
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'push'
      - 'europe-west4-docker.pkg.dev/cloud-computing-resit-90139/trainingsapp/trainingsapp:latest'

  # Deploy to Cloud Run
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: gcloud
    args:
      - 'run'
      - 'deploy'
      - 'trainingsapp'
      - '--image'
      - 'europe-west4-docker.pkg.dev/cloud-computing-resit-90139/trainingsapp/trainingsapp:latest'
      - '--region'
      - 'europe-west4'
      - '--platform'
      - 'managed'
      - '--allow-unauthenticated'
      - '--set-env-vars'
      - 'CLOUD_SQL_INSTANCE_CONNECTION_NAME=$_CLOUD_SQL_INSTANCE,DB_NAME=$_DB_NAME,DB_USER=$_DB_USER'
      - '--set-secrets'
      - 'DB_PASS=db-password:latest'
      - '--memory'
      - '512Mi'
      - '--cpu'
      - '1'
      - '--min-instances'
      - '0'
      - '--max-instances'
      - '10'
      - '--add-cloudsql-instances'
      - '$_CLOUD_SQL_INSTANCE'

substitutions:
  _CLOUD_SQL_INSTANCE: 'cloud-computing-resit-90139:europe-west4:trainingsapp-db'
  _DB_NAME: 'trainingsapp'
  _DB_USER: 'trainingsapp_user'

images:
  - 'europe-west4-docker.pkg.dev/cloud-computing-resit-90139/trainingsapp/trainingsapp:latest'

options:
  logging: CLOUD_LOGGING_ONLY
